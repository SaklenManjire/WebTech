import { Routes } from '@angular/router';
import { ComponentVisualizer } from './features/component-visualizer/component-visualizer';

export const routes: Routes = [
  {
    path: '',
    component: ComponentVisualizer
  }
];